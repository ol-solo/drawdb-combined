# DrawDB Configuration
# Copy this file to .env and configure as needed

# Server Configuration
PORT=5000
NODE_ENV=production

# Sharing storage provider (optional)
# - auto: use GitLab repo provider if configured, otherwise GitHub Gists
# - gitlab-repo: always use GitLab Project Repository
# - github: always use GitHub Gists
# SHARE_PROVIDER=auto

# GitLab Project Repository (recommended for sharing/versioning)
# Requires a dedicated GitLab project (e.g. drawdb-shares)
# GITLAB_BASE_URL=https://gitlab.com
# GITLAB_TOKEN=your_gitlab_token_here
# GITLAB_PROJECT_ID=123456
# GITLAB_REF=main
# GITLAB_SHARES_PATH_PREFIX=shares

# GitHub Token for Gist operations (optional fallback)
# GITHUB_TOKEN=your_github_token_here

# Email Configuration (optional - only if you need email features)
# MAIL_SERVICE=gmail
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password

# CORS - Comma-separated allowed origins (optional)
# CLIENT_URLS=https://mycompany.com,https://www.mycompany.com

# Proxy Configuration for Internet Access (optional - only if behind proxy)
# HTTP_PROXY=http://proxy.example.com:443
# HTTPS_PROXY=http://proxy.example.com:443
# http_proxy=http://proxy.example.com:443
# https_proxy=http://proxy.example.com:443

# Server Name for Nginx (optional - defaults to _ which accepts any domain)
# Set this to your domain name for better security (e.g., mycompany.com)
# SERVER_NAME=mycompany.com
