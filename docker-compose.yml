services:
  drawdb-combined:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: drawdb-combined
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    networks:
      - default

networks:
  default:
    driver: bridge

